{
  "name": "mdpeek-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Generative-UI Preact frontend for markdown-peek (Layer 3)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.24.0",
    "@preact/signals": "^1.3.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
